# Google OAuth credentials (from Google Cloud Console)
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# Google Device Access Project ID (from https://console.nest.google.com/device-access)
NEST_PROJECT_ID=your-device-access-project-id

# OAuth refresh token (obtained by running scripts/get_oauth_token.py)
GOOGLE_REFRESH_TOKEN=your-refresh-token

# Hospitable iCal checkout calendar URL
ICAL_URL=https://api.hospitable.com/v1/team.ics?key=xxx&token=xxx

# Nest device IDs to control (comma-separated, leave empty for all)
NEST_DEVICE_IDS=

# Polling settings (optional)
POLL_INTERVAL_MINUTES=10
CHECKOUT_BUFFER_MINUTES=30
TRIGGER_KEYWORD=TURN_OFF_THERMOSTATS
LOG_LEVEL=INFO

# Scheduled ECO mode - set thermostats to eco at fixed daily times (optional)
# Only applies when thermostat is ON; skips if already OFF (e.g. after checkout)
ECO_SCHEDULE_ENABLED=false
ECO_DAYTIME_HOUR=11
ECO_NIGHTTIME_HOUR=23
ECO_TIMEZONE=America/Los_Angeles
